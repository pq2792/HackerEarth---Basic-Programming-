#include <bits/stdc++.h>
using namespace std;
int main() {
	char s[100];
	cin>>s;
	int l=strlen(s);
	for(int i=0;i<l;i++)
	{
		if(s[i]>='A' && s[i]<='Z'){
			s[i]=s[i]+32;
		}
		else{
			s[i]=s[i]-32;
		}
		cout<<s[i];
	}
}
