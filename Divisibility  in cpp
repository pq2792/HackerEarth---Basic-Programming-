#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin>>n;
	long a[n];
	long ans=0;
	int rem;
	int i;
	for(auto i=0;i<n;i++){
		cin>>a[i];
	}
	for(auto j=0;j<n;j++){
		ans=(ans*10+(a[j]%10))%10;
	}
	if(ans%10==0){
		cout<<"Yes";
	}
	else{
		cout<<"No";
	}
}
